# from lingcorp import Annotator, UniParser, run_pipeline

# You can define your own parser classes here, inheriting from Parser
# Then call run_pipeline as shown below

# morpho = UniParser(analyzer="path/to/analyzer", name="a_name")

# parser_list = {
#     "unparsed.csv": {"output_file": "parsed.csv", "parsers": [morpho]}
# }

# run_pipeline(parser_list)
